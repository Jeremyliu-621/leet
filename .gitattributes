# Normalize line endings: LF in the repo, regardless of OS checkout
* text=auto eol=lf

# Binary assets — never normalize
*.png  binary
*.ico  binary
*.jpg  binary
*.woff binary
*.woff2 binary
*.wasm binary
*.zip  binary
