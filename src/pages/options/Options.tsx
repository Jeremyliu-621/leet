/**
 * Settings page. Phase 8 builds the full options UI (block rules, keyword
 * triggers, difficulty, categories, timings, locks, strict mode, sync status).
 */
export function Options() {
  return (
    <main className="mx-auto min-h-full max-w-3xl bg-bg px-8 py-12 text-text">
      <h1 className="text-lg font-semibold tracking-tight">LeetLock — Settings</h1>
      <p className="mt-2 text-sm text-muted">The settings UI is built in Phase 8.</p>
    </main>
  );
}
