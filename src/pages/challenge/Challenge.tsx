/**
 * The coding-interview gate. Phases 3–4 build the runner integration and the
 * full challenge UI (problem panel, editor, test cases, timer, verdict).
 */
export function Challenge() {
  return (
    <main className="min-h-full bg-bg px-8 py-12 text-text">
      <h1 className="text-lg font-semibold tracking-tight">LeetLock — Challenge</h1>
      <p className="mt-2 text-sm text-muted">The challenge screen is built in Phases 3–4.</p>
    </main>
  );
}
